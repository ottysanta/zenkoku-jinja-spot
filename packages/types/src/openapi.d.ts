/**
 * このファイルは `npm -w @shrine-spots/types run generate`（または `generate:file`）で
 * 上書き生成される自動生成ファイルです。**手で編集しないでください**。
 *
 * ここにある定義は初回生成前でも `packages/types/src/index.ts` がコンパイルできるようにするための
 * スタブです。実際の `paths` / `components` / `operations` は生成後に置き換わります。
 */

export interface paths {}

export interface components {
  schemas: Record<string, unknown>;
}

export interface operations {}

export type webhooks = Record<string, never>;

export type $defs = Record<string, never>;
